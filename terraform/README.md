# Terraform Infrastructure

This directory contains the Terraform configuration for provisioning the Agentic-DevTool-CLI infrastructure.

## Getting Started

1. Install Terraform.
2. Initialize: `terraform init`
3. Plan: `terraform plan`
4. Apply: `terraform apply`
