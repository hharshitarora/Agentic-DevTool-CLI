{
  "name": "agentic-devtool-cli",
  "version": "1.0.0",
  "description": "Node.js CLI project with Commander.js for observability and secret management.",
  "main": "dist/index.js",
  "bin": {
    "agentic-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}